# Fix ChatAddDialog.vue — Use v-model

## Fix
`resources/js/components/ChatAddDialog.vue`:
- Remove `<Form>` wrapper (line 23) and `</Form>` (line 64)
- Remove `<VeeField>` wrapper (line 41) and `</VeeField>` (line 55)
- Add `import { ref } from "vue"` and a `const username = ref("")` in script
- Add `v-model="username"` to the Input component
