# Landing Page for /

## Approach
Redirect `/` to `/login`. No landing page needed — chat apps don't use them.

## Changes

### `routes/web.php`
- Add `Route::get('/', fn () => redirect()->route('login.show'));`

One line. Done.
